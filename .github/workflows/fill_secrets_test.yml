name: Copy file to remote server

on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Copy file to remote server
      env:
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      run: |
        mkdir -p ~/.ssh
        echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H 52.74.36.87 >> ~/.ssh/known_hosts
        scp -P 22 -r package.json eugenechua@52.74.36.87:/home/eugenechua/

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install sshpass
      run: sudo apt-get install -y sshpass
    - name: Copy file using SCP with password
      env:
        SSH_PASSWORD: "!20240221eugeneIGS"
      run: sshpass -p $SSH_PASSWORD scp -o StrictHostKeyChecking=no -P 22 package.json eugenechua@52.74.36.87:/home/eugenechua/
